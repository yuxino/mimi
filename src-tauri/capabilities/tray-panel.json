{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-panel-window",
  "description": "The tray panel may size itself and operate sessions, presentation preferences, and app actions.",
  "windows": ["tray-panel"],
  "permissions": [
    "core:window:allow-set-size",
    "app-tray-panel"
  ]
}
